/**
 * Fichier principal des design tokens
 * Exporte tous les tokens pour un accès centralisé
 */

import { colors } from './colors';
import { typography } from './typography';
import { spacing, radius, shadows, transitions } from './layout';

export const designTokens = {
  colors,
  typography,
  spacing,
  radius,
  shadows,
  transitions,
};

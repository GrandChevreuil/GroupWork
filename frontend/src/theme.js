import { createTheme } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'blue',
  primaryShade: 6,
  colors: {
    // coming soon
  },
  fontFamily: 'Inter, sans-serif',
   // coming spoon
  components: {
    // coming fork
  },
});
